export type userInterface = {
    id?: string,
    email: string,
    phoneNumber: string,
    password: string,

    name?: string,
    gender?: string,
    dob?: string,

    createdAt: string,
    updatedAt: string,
}
  